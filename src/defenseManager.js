function defenseManager(room, creeps, towers) {

    /* 
        -Detects attacks and attack strength, dictates response

        -terminal request resources, defender requirements,
            continue spawning remote creeps, make help request

        -Gives targets to defenders and towers
    */

    let roomTowerDamage = function(roomPosition) {

        let totalDamage = 0

        for (let tower of towers) {

            totalDamage += 0
        }

        return totalDamage
    }
}

module.exports = defenseManager