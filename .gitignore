# non-public features
collectivizer.ts

# user-custom code

src/other/userScript/*
!userScript.example.ts
src/settings.ts
.screeps.yaml

# wasm-pack always puts a .gitignore in here; if you want to not ignore it,
# you'll need to delete it within the dir (for now, see
# https://github.com/rustwasm/wasm-pack/pull/1131)
src/wasm/src
Cargo.lock
Cargo.toml

# wasm output directories
src/wasm/target


# Build
dist
logs
main.js

# User's Push Information
screeps.json
config.yml

# Visual Studio cache/options directory
.vs

# npm
node_modules

# Other Stuff
.env
log.log
push.log
/.VSCodeCounter
tmp-test-server
coverage
.envrc

.idea/
