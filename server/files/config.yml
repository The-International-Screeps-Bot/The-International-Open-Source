steamKey: 71148C12ACC4051B2DA04F3739596E80
mods:
- screepsmod-auth
- screepsmod-admin-utils
- screepsmod-server-stats
# - screepsmod-mongo  # You must install and start `mongodb` and `redis` before this mod will work
bots:
  bot: ../../../dist
serverConfig: # This section requires screepsmod-admin-utils to work
  welcomeText:  |
    <h1 style="text-align: center;">Performance server</h1>
env:
  backend:
    cli_host: 0.0.0.0
    cli_port: 21026
